# DispatchTalon Route And Domain Plan

Date: 2026-05-14

## Executive Verdict

Keep the live routes and deployment names stable during this brand migration. The visible product name becomes DispatchTalon, but `/liftiq/` and `liftiq-pilot.fly.dev` remain in place until a separate route/domain migration is planned.

## Current Routes

Public:

- `https://pressuresystems.au/liftiq/`
- `https://pressuresystems.au/liftiq/how-it-works/`
- `https://pressuresystems.au/liftiq/modules/`
- `https://pressuresystems.au/liftiq/contact/`
- `https://pressuresystems.au/liftiq/design-partner/`
- `https://pressuresystems.au/liftiq/dispatch-blind-spot-diagnostic/`
- `https://pressuresystems.au/liftiq/executive-briefing/`
- `https://pressuresystems.au/liftiq/sample-preview/`

Portal:

- `https://liftiq-pilot.fly.dev/console/`

## Future Routes

Recommended future public route:

- `https://pressuresystems.au/dispatchtalon/`

Recommended future portal route:

- Use a branded domain or subdomain only after legal/domain review, for example `portal.pressuresystems.au` or a DispatchTalon-specific route.

Do not rename the Fly app in this pass.

## Redirect Plan

When the future route is approved:

1. Add `/dispatchtalon/` pages.
2. Keep `/liftiq/` as a 301 redirect or transitional route.
3. Maintain canonical tags on the new route.
4. Update sitemap after the new route is live.
5. Keep old route operational long enough for ads, emails, and external links to age out.

## SEO Considerations

- Current `/liftiq/` links should not break.
- Public copy can display DispatchTalon on the old route during the transition.
- Do not publish trademark-risk reasoning on public pages.
- Use one transition note only on the main product page.

## Domain Reservation Recommendation

Reserve before public scale if attorney advice supports the name:

- `dispatchtalon.com`
- `dispatchtalon.com.au`
- `dispatchtalon.au`

Also screen:

- `pressuredispatchtalon.com.au`
- `dispatchtalonapp.com`
- relevant social handles.

## Social Handle Recommendation

Reserve:

- LinkedIn page slug.
- YouTube handle if walkthroughs are public.
- Email alias under the Pressure Systems domain.

Do not create new public social pages until attorney and domain advice are aligned.
